 ```diff
--- a/frontend/src/services/api.ts
+++ b/frontend/src/services/api.ts
@@ -1,4 +1,4 @@
 /**
  * @fileoverview Legacy API service layer.
  *
  * WARNING: This file was generated by the OpenAPI code generator (v5.4.0)
@@ -17,6 +17,7 @@
  */
 
 import { $httpLegacy, legacyToJson } from '../utils/legacyCompat';
+import { useAuthStore } from '../stores/authStore';
 
 // Base URL for API requests. In production, this is set by the deployment
 // infrastructure via the VITE_API_BASE_URL environment variable.
@@ -55,6 +56,16 @@
 // been updated. We send both the legacy and new auth headers.
 const LEGACY_API_KEY_HEADER = 'X-API-Key';
 
+// Token refresh endpoint path. Used to detect when we're already trying to refresh
+// to avoid infinite retry loops.
+const REFRESH_ENDPOINT = '/auth/refresh';
+
+// Guard for concurrent token refresh attempts. Multiple 401 responses should
+// share a single refresh operation.
+let refreshPromise: Promise<boolean> | null = null;
+
+// Track if a request has already been retried after refresh to prevent loops.
+const retriedAfterRefresh = new WeakSet<Request>();
+
 // ---------------------------------------------------------------------------
 // TYPES
 // ---------------------------------------------------------------------------
@@ -108,6 +119,16 @@
   suggestion?: string;
 }
 
+export class AuthenticationError extends Error {
+  code: number;
+  
+  constructor(message: string, code: number = 401) {
+    super(message);
+    this.name = 'AuthenticationError';
+    this.code = code;
+  }
+}
+
 export interface RequestConfig {
   timeout?: number;
   retries?: number;
@@ -117,3 +138,268 @@
   responseType?: 'json' | 'text' | 'blob';
   withCredentials?: boolean;
   // Legacy options that 
+
+// ---------------------------------------------------------------------------
+// AUTH STATE MANAGEMENT
+// ---------------------------------------------------------------------------
+
+/**
+ * Get the current access token from auth store.
+ */
+function getAccessToken(): string | null {
+  try {
+    const authStore = useAuthStore.getState?.();
+    return authStore?.accessToken ?? null;
+  } catch {
+    return null;
+  }
+}
+
+/**
+ * Get the current refresh token from auth store.
+ */
+function getRefreshToken(): string | null {
+  try {
+    const authStore = useAuthStore.getState?.();
+    return authStore?.refreshToken ?? null;
+  } catch {
+    return null;
+  }
+}
+
+/**
+ * Update tokens in auth store after successful refresh.
+ */
+function setTokens(accessToken: string, refreshToken: string): void {
+  try {
+    const authStore = useAuthStore.getState?.();
+    authStore?.setTokens?.(accessToken, refreshToken);
+  } catch {
+    // Auth store not available, ignore
+  }
+}
+
+/**
+ * Clear auth state on refresh failure.
+ */
+function clearAuth(): void {
+  try {
+    const authStore = useAuthStore.getState?.();
+    authStore?.logout?.();
+  } catch {
+    // Auth store not available, ignore
+  }
+}
+
+// ---------------------------------------------------------------------------
+// TOKEN REFRESH GUARD
+// ---------------------------------------------------------------------------
+
+/**
+ * Perform the actual token refresh request.
+ */
+async function doRefreshToken(): Promise<boolean> {
+  const refreshToken = getRefreshToken();
+  
+  if (!refreshToken) {
+    clearAuth();
+    return false;
+  }
+
+  try {
+    const response = await fetch(`${API_BASE_URL}${REFRESH_ENDPOINT}`, {
+      method: 'POST',
+      headers: {
+        'Content-Type': 'application/json',
+        'Authorization': `Bearer ${refreshToken}`,
+      },
+      body: JSON.stringify({ refreshToken }),
+    });
+
+    // Don't retry if refresh endpoint returns 401/403
+    if (response.status === 401 || response.status === 403) {
+      clearAuth();
+      throw new AuthenticationError('Refresh token expired or invalid', response.status);
+    }
+
+    if (!response.ok) {
+      clearAuth();
+      throw new AuthenticationError(`Token refresh failed: ${response.statusText}`, response.status);
+    }
+
+    const data = await response.json();
+    
+    if (!data.accessToken || !data.refreshToken) {
+      clearAuth();
+      throw new AuthenticationError('Invalid refresh response: missing tokens');
+    }
+
+    setTokens(data.accessToken, data.refreshToken);
+    return true;
+  } catch (error) {
+    if (error instanceof AuthenticationError) {
+      throw error;
+    }
+    clearAuth();
+    throw new AuthenticationError(error instanceof Error ? error.message : 'Token refresh failed');
+  }
+}
+
+/**
+ * Single-flight token refresh. Multiple concurrent calls will share
+ * the same refresh promise.
+ */
+function refreshTokenWithGuard(): Promise<boolean> {
+  if (refreshPromise) {
+    return refreshPromise;
+  }
+
+  refreshPromise = doRefreshToken().finally(() => {
+    refreshPromise = null;
+  });
+
+  return refreshPromise;
+}
+
+// ---------------------------------------------------------------------------
+// REQUEST HELPERS
+// ---------------------------------------------------------------------------
+
+/**
+ * Check if a request is the refresh endpoint itself.
+ */
+function isRefreshRequest(request: Request): boolean {
+  return request.url.includes(REFRESH_ENDPOINT);
+}
+
+/**
+ * Clone a request with a new auth header.
+ */
+function cloneRequestWithToken(request: Request, token: string): Request {
+  const headers = new Headers(request.headers);
+  headers.set('Authorization', `Bearer ${token}`);
+  
+  return new Request(request, {
+    headers,
+  });
+}
+
+/**
+ * Execute a fetch request with timeout support.
+ */
+async function fetchWithTimeout(
+  request: Request,
+  timeoutMs: number
+): Promise<Response> {
+  const controller = new AbortController();
+  const timeoutId = setTimeout(() => controller.abort(), timeoutMs);
+  
+  try {
+    const response = await fetch(request, { signal: controller.signal });
+    clearTimeout(timeoutId);
+    return response;
+  } catch (error) {
+    clearTimeout(timeoutId);
+    throw error;
+  }
+}
+
+// ---------------------------------------------------------------------------
+// MAIN API CLIENT
+// ---------------------------------------------------------------------------
+
+
+/**
+ * Make an API request with automatic token refresh and retry.
+ * 
+ * Features:
+ * - Deduplicates concurrent refresh attempts via single-flight guard
+ * - Retries original request exactly once after successful refresh
+ * - Clears auth state and